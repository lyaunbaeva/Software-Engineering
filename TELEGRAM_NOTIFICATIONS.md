# 📱 Уведомления в Telegram

## Описание

Проект интегрирован с Telegram для отправки уведомлений о выполненных вычислениях через API.

## Возможности

- Автоматическая отправка уведомлений в Telegram при вычислениях через API
- Простая настройка через переменные окружения
- Обработка ошибок без прерывания работы API

---

## Настройка

### Шаг 1: Создание Telegram-бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный токен

### Шаг 2: Получение Chat ID

**Способ 1: Через скрипт**
```bash
TELEGRAM_BOT_TOKEN=your_token python telegram_notify.py
```
Отправьте любое сообщение боту, и скрипт покажет ваш Chat ID.

**Способ 2: Через бота @userinfobot**
1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Бот покажет ваш Chat ID

### Шаг 3: Настройка переменных окружения

```bash
# Токен бота (обязательно)
export TELEGRAM_BOT_TOKEN=your_bot_token_here

# ID чата для уведомлений (обязательно)
export TELEGRAM_CHAT_ID=your_chat_id

# Включить отправку уведомлений (обязательно)
export TELEGRAM_ENABLED=true
```

---

## Использование

### Запуск API с уведомлениями

```bash
# Установите переменные окружения
export TELEGRAM_BOT_TOKEN=your_token
export TELEGRAM_CHAT_ID=your_chat_id
export TELEGRAM_ENABLED=true

# Запустите API
python api.py
```

### Пример запроса с уведомлением

```bash
curl -X POST http://localhost:5001/api/calculate \
     -H "Content-Type: application/json" \
     -H "X-API-Key: secret_key_12345" \
     -d '{"operation": "add", "a": 10, "b": 5}'
```

После выполнения вычисления вы получите уведомление в Telegram:

```
🧮 Новое вычисление выполнено

10 + 5 = 15

Результат: 15
```

### Указание Chat ID в запросе

Можно указать Chat ID в заголовке запроса:

```bash
curl -X POST http://localhost:5001/api/calculate \
     -H "Content-Type: application/json" \
     -H "X-API-Key: secret_key_12345" \
     -H "X-Telegram-Chat-ID: your_chat_id" \
     -d '{"operation": "multiply", "a": 7, "b": 6}'
```

---

## Тестирование

### 1. Проверка настройки

Убедитесь, что все переменные окружения установлены:
```bash
echo $TELEGRAM_BOT_TOKEN
echo $TELEGRAM_CHAT_ID
echo $TELEGRAM_ENABLED
```

### 2. Запуск API

```bash
python api.py
```

### 3. Выполнение тестового вычисления

```bash
curl -X POST http://localhost:5001/api/calculate \
     -H "Content-Type: application/json" \
     -H "X-API-Key: secret_key_12345" \
     -d '{"operation": "add", "a": 10, "b": 5}'
```

### 4. Проверка Telegram

Проверьте, что уведомление пришло в Telegram.

---

## Обработка ошибок

### Отсутствие токена или Chat ID

Если `TELEGRAM_BOT_TOKEN` или `TELEGRAM_CHAT_ID` не установлены, уведомления не будут отправляться. Ошибки логируются, но не прерывают работу API.

### Ошибки сети

При ошибках сети (недоступность Telegram API) ошибки логируются, но не влияют на работу основного функционала API.

### Дублирование уведомлений

Уведомления отправляются только при успешном выполнении вычисления. Каждое вычисление создает отдельное уведомление.

---

## Пример отчета

**Интеграция выполнена:**

✅ Модуль отправки уведомлений в Telegram реализован
✅ Интеграция с API выполнена - уведомления отправляются автоматически
✅ Обработка ошибок реализована корректно
✅ Тестирование пройдено успешно

**Использованные технологии:**
- python-telegram-bot (SDK для Telegram Bot API)
- Асинхронное программирование (asyncio)
- Интеграция с Flask API

**Результаты тестирования:**
- Уведомления отправляются при вычислениях через API
- Ошибки обрабатываются без прерывания работы системы
- Дублирование уведомлений не происходит

---

*Версия: 1.0*  
*Дата: 2026*
